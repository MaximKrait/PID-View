# PID-View
Simple C utility for viewing basic Linux process information such as name, state, and priority using the `/proc` filesystem.

## Building
```shell
make
```
## Run
```shell
./build/release/pid-view PID
```

